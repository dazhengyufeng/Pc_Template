import Vue from "vue";
import Vuex from "vuex";
Vue.use(Vuex);
import * as type from "./Mutation/index";


const state = {
}

const getters = {
}

const mutations = {
}

const actions = {
}

export default new Vuex.Store({
  state,
  mutations,
  actions,
  getters,
  modules: {
  }
})

